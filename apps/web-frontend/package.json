{
  "name": "web-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Static status page used to demo CI/CD and GitOps",
  "scripts": {
    "test": "node --test test/*.test.js",
    "lint": "node --check public/status.js"
  }
}
