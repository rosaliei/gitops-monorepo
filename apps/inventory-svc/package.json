{
  "name": "inventory-svc",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny stock service used to demo CI/CD and GitOps",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js",
    "lint": "node --check src/app.js && node --check src/server.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "prom-client": "15.1.3"
  }
}
