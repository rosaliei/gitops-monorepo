# prod: only the 'promote' workflow changes this, after approval (merge = deploy).
# Chart defaults: charts/app/values.yaml
environment: prod

image:
  repository: ghcr.io/rosaliei/web-frontend
  tag: "0.1.0"
  digest: ""

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 4

probes:
  readiness: /healthz

env:
  ORDERS_API_HOST: orders-api.demo-prod.svc.cluster.local
  INVENTORY_HOST: inventory-svc.demo-prod.svc.cluster.local

writableDirs:
  - /tmp
  - /etc/nginx/conf.d

resources:
  requests:
    cpu: 100m
    memory: 96Mi
  limits:
    memory: 192Mi
