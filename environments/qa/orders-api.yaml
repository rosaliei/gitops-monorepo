# qa: CI opens a PR here when a new version is released (merge = deploy).
# Chart defaults: charts/app/values.yaml
environment: qa

image:
  repository: ghcr.io/rosaliei/orders-api
  tag: "0.1.0"
  digest: "sha256:ba78640858ecc149c1de8974452a46c11933756361a442614c254166f7eb7b61"

replicaCount: 1

env:
  LOG_LEVEL: debug

secretEnv:
  enabled: true
  data:
    API_KEY: qa/orders-api/api-key

metrics:
  enabled: true
