# dev: CI commits a new build here on every merge to main (automatic).
# Chart defaults: charts/app/values.yaml
environment: dev

image:
  repository: ghcr.io/rosaliei/orders-api
  tag: "0.1.0"
  digest: ""

replicaCount: 1

env:
  LOG_LEVEL: debug

secretEnv:
  enabled: true
  data:
    API_KEY: dev/orders-api/api-key

metrics:
  enabled: true
